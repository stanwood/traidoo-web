import { createStyles, makeStyles, Theme } from "@material-ui/core/styles";

const useSellerProductDetailsPageStyles = makeStyles((theme: Theme) =>
  createStyles({
    container: {
      height: "100%",
    },
  })
);

export default useSellerProductDetailsPageStyles;
